# UI

> 视觉语言、组件视觉、页面模式。

## 文件

- [`rules.md`](./rules.md) — UI 总纲（设计系统选定后填充）

## 当前状态

设计系统候选还在产出中（见 `flow-project-bootstrap` 的 ui-ux-pro-max 步骤）。在确定方向前：

- **不要先写大量 UI 组件**
- 先实现纯逻辑层（store、storage、injector）
- 等设计选定后再统一搭组件库
