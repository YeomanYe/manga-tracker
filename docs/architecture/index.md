# Architecture

> 代码放哪、模块怎么拆、依赖往哪个方向。

## 文件

- [`rules.md`](./rules.md) — 架构总纲：包边界、依赖方向、合规底线
- [`packages.md`](./packages.md) — `apps/` 和 `packages/` 各自子目录约定 + 源规则 schema + 跨端代码共享判断

## 阅读顺序

新人 → `rules.md` → `packages.md` → 需要写新功能时再回来确认。
